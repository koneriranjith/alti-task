import React from "react";

interface HomeProps {
  // Define your props here if needed
}

const Home: React.FC<HomeProps> = (props) => {
  return (
    <div>
      <p>Home</p>
    </div>
  );
};

export default Home;
